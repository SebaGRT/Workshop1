# Business Intelligence · Workshop 1

The submission snapshot is in [`GROUP_XX_W1/`](GROUP_XX_W1/README.md). It follows the professor's required structure: one root folder with `README.md`, `report/`, `analysis/`, and `data/`. The ready-to-inspect archive is `GROUP_XX_W1.zip`.

`XX` is a placeholder because the group's two-digit ID has not been assigned. Replace it consistently in the folder, archive, and report filenames before uploading to Canvas.

The directories `Urgencias/`, `REM/`, `Establecimientos/`, and `GRD/` at repository root are the local source collection. Large original datasets stay here for exact reproduction and are excluded from the submission archive. `FUENTES.csv`, `FUENTES.json`, `DESCARGAS_REALIZADAS.csv`, and `descargar_datos_W1_actualizado.py` document that collection. The submission's `data/source_manifest.csv` lists only the sources used by the two analyses, with download dates and SHA-256 hashes.

To inspect the submitted work, start with the report PDF, then the saved notebook outputs and reduced data described in the package README. The professor's brief remains at repository root and is also included in the submission's `data/reference/` folder.

The formatted English report is available as a [Google Doc](https://docs.google.com/document/d/1yr28MOk6eWTMOoFIpBCnv9CIzfOCUTrpTGP9T3mb7Gs/edit) and in the package as PDF and DOCX.
